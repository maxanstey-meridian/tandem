{
  "name": "tandem-node-api-productization",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "../examples/*/typescript"
  ],
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build:bridge": "dotnet publish bridge/Tandem.NodeApiSpike.Bridge.csproj -r osx-arm64 --self-contained false --output .runtime-publish && node scripts/stage-runtime.mjs",
    "build": "pnpm clean && pnpm build:bridge && tsc -p packages/sdk/tsconfig.json && node scripts/verify-package.mjs",
    "typecheck": "tsc -p tsconfig.json && tsc -p tests/types/negative/tsconfig.json",
    "lint": "oxlint . && pnpm --filter '../examples/**' exec oxlint -c \"$PWD/.oxlintrc.json\" src",
    "format": "oxfmt --write . && pnpm --filter '../examples/**' exec oxfmt -c \"$PWD/.oxfmtrc.json\" --write src package.json",
    "format:check": "oxfmt --check . && pnpm --filter '../examples/**' exec oxfmt -c \"$PWD/.oxfmtrc.json\" --check src package.json",
    "check": "pnpm build && pnpm typecheck && pnpm lint && pnpm format:check",
    "test:bridge": "dotnet test bridge-tests/Tandem.NodeApiSpike.Bridge.Tests.csproj",
    "test": "pnpm check && pnpm test:bridge && node --import tsx --test tests/*.test.mjs && pnpm pack-consumer",
    "pack-consumer": "node scripts/pack-consumer.mjs",
    "run:code-writer": "pnpm build && pnpm --filter tandem-code-writer-example run run",
    "run:debate": "pnpm build && pnpm --filter tandem-debate-example run run",
    "run:songwriter": "pnpm build && pnpm --filter tandem-songwriter-example run run"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tandem/sdk": "workspace:*",
    "@types/node": "^24.10.1",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@11.20.0"
}
