# Todo API

A minimal in-memory todo API in TypeScript.

## Structure

- `src/types.ts` — `Todo` interface
- `src/store.ts` — in-memory `TodoStore` backed by `Map`
- `src/service.ts` — `TodoService` providing CRUD operations

## Usage

```bash
npm install
npm run build
npm start
```