# Tandem

Tandem is being rebuilt as a configurable, durable pipeline for agentic software
work.

Read the architecture and implementation plans in [`docs/README.md`](docs/README.md).

The repository currently contains only the .NET scaffold. The application is not
implemented.

## Development

```sh
dotnet tool restore
task check
```
